# Copy to `.env` and adjust.

# Comma-separated origins when using credentials (browser + cookies).
# CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# CORS_ALLOW_CREDENTIALS=true

# Optional absolute or relative paths for runtime file storage.
# UPLOAD_DIR=uploads
# OUTPUT_DIR=outputs
